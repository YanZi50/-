# 信息流素材一键拼接程序 · 需求文档

版本：v1.0  
日期：2026-09-05  
状态：已确认

## 1. 项目目标

开发一个 Windows 桌面极简程序，用于信息流视频素材的批量拼接。用户准备两个素材文件夹：

- 开头素材库：只存放可以作为片头的视频；
- 结尾素材库：只存放可以作为片尾的视频。

程序从两个素材库中自动匹配开头和结尾，生成 1–200 条 MP4 成片。

核心操作目标：**选择文件夹、设置输出参数、点击开始生成，即可批量得到多条不同组合的视频。**

## 2. 目标用户

- 信息流广告投放人员；
- 短视频矩阵运营；
- 需要快速批量生成不同素材组合的内容团队；
- 个人创作者。

## 3. 术语定义

| 术语 | 说明 |
| --- | --- |
| 开头素材库 | 存放开头视频的文件夹 |
| 结尾素材库 | 存放结尾视频的文件夹 |
| 固定开头 | 从开头素材库中手动指定的一个视频，所有成片都使用它作为开头 |
| 固定结尾 | 从结尾素材库中手动指定的一个视频，所有成片都使用它作为结尾 |
| 成片 | 一条完整输出视频，当前版本由“开头视频 + 结尾视频”拼接而成 |
| 出片数量 | 一次任务要生成多少条成片 |
| 目标时长 | 每条成片希望达到的时长限制，可选不限制、15s、25s、30s |

## 4. 需求澄清与冲突说明

### 4.1 与原始需求的对应关系

| 原始说法 | 当前说法 | 结论 |
| --- | --- | --- |
| 两个文件选择，一个开头，一个结尾 | 开头文件夹、结尾文件夹 | 不冲突，属于从“单文件”扩展为“文件夹素材库” |
| 不固定开头结尾 | 可选固定开头或固定结尾 | 不冲突，属于两种可选模式 |
| 一键拼合不同素材 | 自动匹配不同开头/结尾 | 一致，程序自动组合 |

### 4.2 已按以下方式理解

- “专长”理解为“转场”。
- BGM 支持本地导入，也支持程序自动配乐。
- 当前版本成片为“开头 + 结尾”两段拼接。

### 4.3 待确认项

已确认：**不需要中间素材**，成片仅由“开头视频 + 结尾视频”两段拼接而成。

## 5. 总体业务流程

```text
1. 选择开头素材库
2. 选择结尾素材库
3. 可选：设置固定开头或固定结尾
4. 设置生成数量、分辨率、目标时长
5. 可选：设置水印、字幕、转场、BGM
6. 点击“开始生成”
7. 程序自动组合素材并输出 MP4
8. 生成过程中可暂停/取消
9. 完成后打开输出目录
```

## 6. 功能需求

### F1 素材库选择

- 用户选择一个开头素材库文件夹。
- 用户选择一个结尾素材库文件夹。
- 程序扫描文件夹内的视频文件，支持常见格式：MP4、MOV、MKV、AVI、TS、M4V 等。
- 文件夹内非视频文件默认忽略。

### F2 固定开头/固定结尾

- 用户可从开头素材库中选择一个视频作为“固定开头”。
- 用户可从结尾素材库中选择一个视频作为“固定结尾”。
- 固定开头、固定结尾均可单独启用，也可以都启用或都不启用。
- 固定开头只能来自开头素材库，固定结尾只能来自结尾素材库。

### F3 自动匹配规则

程序按以下规则生成成片：

| 模式 | 生成逻辑 |
| --- | --- |
| 固定开头 + 不固定结尾 | 每条成片使用固定开头，结尾从结尾素材库中自动匹配不同素材 |
| 固定结尾 + 不固定开头 | 每条成片使用固定结尾，开头从开头素材库中自动匹配不同素材 |
| 固定开头 + 固定结尾 | 只生成 1 条成片，片头片尾均为固定文件 |
| 不固定开头 + 不固定结尾 | 每条成片分别从开头库、结尾库中随机选择一条进行组合 |

匹配原则：

- 在出片数量范围内，优先使用“交叉组合”策略，尽量让每条成片的开头/结尾组合不重复；
- 当可组合数量不足时，允许循环使用，但仍会尽量降低重复率；
- 组合数量受用户设置的出片数量限制。

### F4 出片数量

- 用户可设置生成数量，范围为 1–200。
- 程序最多生成 200 条。
- 如果模式为“固定开头 + 固定结尾”，出片数量自动变为 1 条。

### F5 输出格式与分辨率

- 输出格式：MP4。
- 可选分辨率：
  - 1080 × 1920，竖屏；
  - 1920 × 1080，横屏。
- 默认帧率：30 FPS，已确认。
- 编码：优先 H.264 + AAC。
- 输出视频统一到用户选择的分辨率。

### F6 目标时长

- 默认：不限制，成片时长 = 开头视频时长 + 结尾视频时长。
- 可选目标时长：
  - 15 秒；
  - 25 秒；
  - 30 秒。

时长处理规则：

- 如果成片总时长小于目标时长，保留原始长度，不强行拉伸；
- 如果成片总时长大于目标时长，优先从结尾部分裁短，使成片不超过目标时长。

### F7 水印

- 用户可选择是否添加水印。
- 水印支持本地图片文件。
- 首版水印位置固定为右下角。
- 支持简单设置水印大小或透明度。

### F8 字幕

- 用户可选择是否添加字幕。
- 字幕采用自动识别方式，从视频语音中识别并生成字幕。
- 首版优先识别中文普通话。
- 使用本地语音识别模型生成字幕，并烧录到视频画面中。

### F9 转场

- 用户可选择是否在开头视频和结尾视频之间添加转场。
- 首版先提供一种通用转场效果，建议为“叠化/淡入淡出”。
- 转场时长默认 0.3 秒。

### F10 BGM

- 用户可选择是否添加背景音乐。
- 支持两种来源：
  1. 用户本地导入音乐文件；
  2. 程序自动配乐，由算法生成一段简单背景音乐。
- BGM 时长与成片时长对齐，超出部分裁掉，不足部分保持原长度。
- 首版支持设置 BGM 音量。

### F11 任务控制

- 支持“开始生成”。
- 生成过程中支持“取消”。
- 取消后，已经完成的文件保留，未开始的素材停止处理。
- 支持“暂停/继续”。

### F12 进度与结果

- 显示当前进度：已完成条数 / 总条数。
- 显示成功数量和失败数量。
- 单条素材失败时，其他素材继续处理。
- 完成后支持“打开输出目录”。

### F13 输出文件命名

- 默认命名规则：`output_001.mp4`、`output_002.mp4`，依次递增。
- 同一输出目录下自动避免覆盖。

## 7. 非功能需求

- **平台**：Windows 10 / Windows 11，64 位。
- **本地处理**：素材不上传服务器。
- **性能**：在素材参数一致的情况下，尽量使用 FFmpeg 无损流复制拼接。
- **稳定性**：200 条任务中单条失败不影响整体流程。
- **可中断**：取消操作应在 2 秒内停止新任务，并尽快结束当前任务。
- **易用性**：主要操作步骤不超过 5 步。

## 8. 界面设计草案

### 主界面区块

1. 素材设置：
   - 开头素材库选择框；
   - 结尾素材库选择框；
   - 固定开头选择；
   - 固定结尾选择。
2. 输出设置：
   - 生成数量；
   - 分辨率；
   - 目标时长；
   - 输出目录。
3. 效果选项：
   - 水印开关及文件；
   - 字幕开关；
   - 转场开关；
   - BGM 开关及来源。
4. 任务区：
   - 开始生成按钮；
   - 暂停/继续按钮；
   - 取消按钮；
   - 进度条；
   - 成功/失败统计。
5. 结果区：
   - 最近一次输出目录；
   - 打开输出目录按钮。

## 9. 业务规则

- 开头视频只能来自开头素材库。
- 结尾视频只能来自结尾素材库。
- 每个成片至少包含一个开头和一个结尾。
- 不固定模式优先随机组合，但避免完全相同的组合重复出现。
- 固定开头 + 固定结尾只生成 1 条。
- 出片数量不能超过 200。
- 用户取消后不得继续自动生成新成片。

## 10. 验收标准

- 能够选择开头文件夹和结尾文件夹并正确读取视频。
- 能够选择固定开头或固定结尾。
- 能够设置 1–200 条生成数量。
- 能够输出 1080×1920 或 1920×1080 的 MP4。
- 能够按“不限制、15s、25s、30s”处理时长。
- 能够选择是否添加水印、字幕、转场、BGM。
- 生成过程中可以取消。
- 取消后已完成文件保留，未完成任务停止。
- 单条失败不影响其他任务。

## 11. MVP 范围与后续版本

### MVP，首版必须实现

- 开头/结尾文件夹选择；
- 固定开头/固定结尾；
- 自动匹配组合；
- 生成数量 1–200；
- 1080×1920 或 1920×1080，MP4；
- 目标时长：不限制、15s、25s、30s；
- 水印开关；
- 转场开关；
- BGM 本地导入或算法生成；
- 任务进度、取消、输出目录。

### 后续版本

- 更多转场类型；
- 更丰富的算法配乐风格；
- 自定义水印位置和尺寸；
- 字幕样式和字体调整；
- 批量去重、更智能的组合策略。

## 12. 需求确认记录

1. 是否需要中间素材：不需要，成片为“开头 + 结尾”两段拼接。
2. 字幕形式：自动识别视频语音并生成字幕。
3. 程序配乐：由算法生成背景音乐。
4. 默认帧率：30 FPS。
5. 不固定开头、不固定结尾时：优先使用交叉组合，降低重复率；组合不足时再循环。
