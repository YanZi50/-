# 信息流素材拼接 MVP 实施计划

## 目标

在现有网页版 MVP 基础上，逐步把稳定性、批量体验、音频处理、智能组合和扩展能力补齐，每一阶段都保持可运行、可回滚、可测试。

## 执行原则

- 每完成一个阶段，立即创建对应的 Git commit；
- 每个阶段都要补充或更新测试；
- 测试和验证全部通过后，才进入下一阶段；
- 优先保证不破坏现有“开头 + 结尾”核心流程。

## 当前进度

- [x] P0：建立完整基线测试
- [x] P1：任务断点续跑、失败重试、日志持久化
- [x] P2：配置保存与加载
- [x] P3：结果明细和失败原因
- [x] P4：任务历史
- [x] P5：音频优化
- [x] P6：输出命名模板
- [x] P7：智能组合与去重
- [x] P8：中间素材池
- [x] P9：生成前预览
- [x] P10：模板管理
- [x] P11：平台尺寸与质量预设
- [ ] P12：自动字幕可选插件

## 阶段总览

| 阶段 | 内容 | 优先级 | 预期价值 |
| --- | --- | --- | --- |
| P0 | 建立完整基线测试 | 最高 | 确保后续改动安全 |
| P1 | 任务断点续跑、失败重试、日志持久化 | 最高 | 解决大批量任务不可恢复问题 |
| P2 | 配置保存与加载 | 高 | 减少重复设置 |
| P3 | 结果明细和失败原因 | 高 | 快速定位问题 |
| P4 | 任务历史 | 中高 | 方便复盘和复用 |
| P5 | 音频优化：音量统一、BGM 淡入淡出 | 中高 | 提高成片质量 |
| P6 | 输出命名模板 | 中 | 方便投放管理 |
| P7 | 智能组合与去重 | 中 | 提高素材差异化 |
| P8 | 中间素材池 | 中 | 从两段拼接升级为信息流混剪 |
| P9 | 生成前预览 | 中 | 避免批量浪费 |
| P10 | 片头片尾/水印/BGM 模板管理 | 中 | 沉淀团队标准 |
| P11 | 平台尺寸与质量预设 | 中低 | 适配多平台 |
| P12 | 自动字幕可选插件 | 中低 | 满足投放字幕需求 |

## P0：建立完整基线测试

### 目标

为现有核心逻辑建立自动化测试，后续每阶段都要在此基础上扩展。

### 内容

1. 测试视频扫描和参数探测；
2. 测试开头 + 结尾批量拼接；
3. 测试固定开头、固定结尾；
4. 测试分辨率统一；
5. 测试目标时长截断；
6. 测试 BGM 本地导入和算法生成；
7. 测试水印添加；
8. 测试取消和失败隔离。

### 验收

- 运行测试脚本全部通过；
- 无人工合成素材即可完成；
- 测试不污染正式输出目录。

## P1：任务断点续跑、失败重试、日志持久化

### 目标

大批量任务中断后可以继续，不重复生成已完成的文件，单条失败可自动重试。

### 内容

1. 开始任务前扫描输出目录；
2. 已存在且完整的输出文件默认跳过；
3. 单条失败自动重试 1–2 次；
4. 失败项单独记录原因；
5. 页面提供“只重试失败项”能力；
6. 日志写入本地 `logs/` 目录；
7. 日志按日期和任务 ID 拆分。

### 验收

- 同一个任务执行两次，第二次不会重复处理已生成文件；
- 注入一个失败素材，程序能重试并记录失败原因；
- 取消后重启任务，能继续处理剩余素材。

## P2：配置保存与加载

### 目标

保存常用设置，避免每次重新选择文件夹和参数。

### 内容

1. 定义 `config.json` 数据结构；
2. 保存素材路径、固定项、数量、分辨率、时长、效果选项；
3. 程序启动时自动读取最近配置；
4. 页面支持“保存配置 / 载入配置 / 恢复默认”。

### 验收

- 保存配置后重启程序，参数自动恢复；
- 载入历史配置后能直接生成任务。

## P3：结果明细和失败原因

### 目标

任务完成后能看到每条成片来源、状态、失败原因和下载入口。

### 内容

1. 后端返回每条任务的结果明细；
2. 页面用表格展示：序号、开头、结尾、状态、输出文件、错误原因；
3. 支持下载单条结果；
4. 失败项可再次加入任务。

### 验收

- 任务完成后结果表格正确显示；
- 失败项有可读原因；
- 下载入口可正常下载。

## P4：任务历史

### 目标

记录每次生成任务，方便复盘和复用。

### 内容

1. 记录任务时间、参数、成功数、失败数、输出目录；
2. 存储到本地 `history/` 目录；
3. 页面增加“任务历史”区域；
4. 支持从历史任务载入配置。

### 验收

- 每次任务结束后自动记录；
- 历史列表可查看、可载入、可打开输出目录。

## P5：音频优化：音量统一、BGM 淡入淡出

### 目标

避免成片音量忽大忽小，BGM 不压过原声。

### 内容

1. 使用 FFmpeg 检测并统一音量为目标响度；
2. BGM 首尾淡入淡出；
3. 支持 BGM 与人声自动闪避；
4. 页面增加“统一音量”和“BGM 淡化”开关。

### 验收

- 多个音量不同的素材输出后响度接近；
- BGM 进入和结束没有明显突兀；
- 开启闪避后，有人声时 BGM 自动降低。

## P6：输出命名模板

### 目标

让输出文件名可读、可管理。

### 内容

1. 支持 `{日期}`、`{序号}`、`{开头}`、`{结尾}` 等变量；
2. 页面提供模板输入框和预览；
3. 保留唯一文件名避免覆盖。

### 验收

- 自定义模板能正确生成文件名；
- 不合法模板有提示；
- 不会覆盖已有文件。

## P7：智能组合与去重

### 目标

减少重复和相似素材组合。

### 内容

1. 记录已使用组合，避免重复；
2. 按文件名关键词、时长、分辨率进行粗去重；
3. 可选随机种子，保证结果可复现；
4. 组合不足时智能循环并降低相邻重复率。

### 验收

- 相同素材池和种子生成结果一致；
- 组合重复率低于随机顺序拼接；
- 出片数量超过唯一组合数时，循环策略合理。

## P8：中间素材池

### 目标

支持“开头 + 中间素材 + 结尾”的三段式信息流结构。

### 内容

1. 增加中间素材文件夹；
2. 支持固定中间素材或随机组合；
3. 支持多段中间素材拼接；
4. 时长限制适配三段结构；
5. 更新需求文档和页面。

### 验收

- 可以选择中间素材池并批量生成；
- 成片结构为开头 + 中间 + 结尾；
- 原有两段模式仍可使用。

## P9：生成前预览

### 目标

避免生成大量废片。

### 内容

1. 生成 1 条预览视频；
2. 页面内嵌播放器预览；
3. 用户确认后再批量生成；
4. 支持预览失败时快速调整。

### 验收

- 可先预览一条再决定是否批量；
- 预览结果和最终成片一致。

## P10：片头片尾/水印/BGM 模板管理

### 目标

把常用配置沉淀成模板。

### 内容

1. 保存片头、片尾、水印、BGM 组合为模板；
2. 模板列表展示和切换；
3. 模板可导入导出 JSON。

### 验收

- 模板能保存、载入、切换；
- 团队间可以共享模板文件。

## P11：平台尺寸与质量预设

### 目标

快速适配不同平台。

### 内容

1. 增加抖音、快手、小红书、B站等预设；
2. 预设包含分辨率、帧率、码率建议；
3. 用户可自定义并保存预设。

### 验收

- 选择预设后参数自动变化；
- 成片符合目标平台基本规格。

## P12：自动字幕可选插件

### 目标

为有字幕需求的用户提供可选能力。

### 内容

1. 将字幕能力做成可选依赖；
2. 检测到依赖后才显示字幕选项；
3. 支持烧录字幕和导出 SRT；
4. 默认便携版不打包字幕模型。

### 验收

- 未安装字幕依赖时不影响核心功能；
- 安装后可以生成字幕；
- 字幕能烧录或导出。

## 实施顺序

1. P0：建立完整基线测试
2. P1：任务断点续跑、失败重试、日志持久化
3. P2：配置保存与加载
4. P3：结果明细和失败原因
5. P4：任务历史
6. P5：音频优化
7. P6：输出命名模板
8. P7：智能组合与去重
9. P8：中间素材池
10. P9：生成前预览
11. P10：模板管理
12. P11：平台预设
13. P12：自动字幕可选插件

## 提交规范

- 每阶段一个或多个 commit；
- commit message 使用 `P{n}: 描述`；
- 每个阶段结束前运行全部测试；
- 测试未通过不得提交。
