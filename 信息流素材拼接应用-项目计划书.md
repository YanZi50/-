# 信息流素材一键拼接应用 · 项目计划书

## 1. 项目概述

本项目拟开发一款面向信息流广告、短视频投放、自媒体矩阵运营的**轻量级桌面素材拼接工具**。核心价值是把“固定片头 + 不同中间素材 + 固定片尾”的重复性拼接工作，压缩为“选择两个文件、点击一次按钮”即可批量完成。

一句话产品定义：

> 选择 1 个片头文件、1 个片尾文件，一键将多段不同素材批量拼成完整成片。

## 2. 需求解读与关键假设

你目前给出的需求是：

- 两个文件选择：一个开头，一个结尾；
- 可以一键拼合不同素材。

为让计划可落地，本计划书基于以下假设：

1. **素材类型以视频为主**，同时兼容图片 + 音频的组合，最终输出视频。
2. **片头和片尾固定**，中间素材可以是一个或多个不同文件。
3. 核心场景是**批量生成**：例如 1 个片头 + 1 个片尾 + 20 个不同中间素材，生成 20 条成片。
4. 第一阶段为 **Windows 桌面应用**，本地处理，不依赖云端账号。
5. 若中间素材与片头、片尾的编码参数一致，优先使用无损快速拼接；参数不一致时自动转码统一。

如果实际需求只是“两个视频前后拼成一个”，那属于本产品的单条拼接子集，仍可在同一应用内实现。

## 3. 市场调研与竞品分析

### 3.1 调研结论

目前市面上**存在相关能力**，但主要分为两类：

- 通用剪辑软件：如剪映、快剪辑、汇帮视频处理大师等，功能全面，但操作链路较长，不是为“批量片头片尾拼接”专门优化。
- 投放/混剪专用工具：如驭影、螃蟹剪辑、超级混剪、创量智投等，功能强大，但通常偏向整套投放流程，学习成本、付费门槛和功能冗余较高。

与你的需求最接近的工具包括：

| 工具 | 类型 | 核心相关能力 | 与需求的差异 |
| --- | --- | --- | --- |
| 剪映 / CapCut | 通用剪辑 | 手动时间轴、片头片尾模板 | 手动为主，批量拼接能力弱 |
| 快剪辑 | 通用剪辑 | 拼接、拆分、掐头去尾 | 需要人工排列时间轴 |
| 汇帮视频处理大师 | 通用视频处理 | 批量添加片头、片尾、合并 | 功能较多，非极简定位 |
| 螃蟹剪辑 | 批量混剪 | 批量智能混剪、片头片尾、模板 | 功能复杂，偏全自动混剪 |
| 驭影 | 批量视频处理 | 批量分组拼接、批量加片头片尾 | 偏矩阵运营，含较多附加功能 |
| 超级混剪 / N片段混剪 | 广告混剪工具 | 按片头、片中、片尾文件夹随机/顺序组合 | 接近需求，但偏文件夹混剪而非单文件极简操作 |
| 创量智投 | 广告投放工具 | 批量加片头片尾、尺寸变更、素材差异化 | 面向投放全链路，门槛较高 |
| video-merger-cli | 命令行工具 | 基于 FFmpeg，支持片头片尾和批量处理 | 适合技术用户，无图形界面 |

### 3.2 市场机会

你的需求虽然技术上不复杂，但存在一个**体验缝隙**：

- 现有工具普遍偏重，需要学习多个功能入口；
- 很多工具绑定云端、账号、积分或整套投放系统；
- “两个文件选择 + 一键拼合不同素材”这种极简、本地、可批量、可快速交付的工作流，仍有独立产品的空间。

因此，本项目建议走**小而专**路线，而不是与剪映、驭影等大而全工具正面竞争。

### 3.3 可复用开源项目与代码

根据 GitHub 和 npm 调研，以下项目可以直接复用或作为开发起点：

| 项目 | 地址 | 许可证/授权 | 可复用价值 | 适用方式 |
| --- | --- | --- | --- | --- |
| video-merger-cli | npm：`@yeah126139163/video-merger-cli` | 需确认 package 内 license | 核心能力就是“固定片头 + 片尾 + 批量合并”，与本需求最接近 | 可直接作为 CLI 后端，或抽取其 FFmpeg 拼接逻辑 |
| jianying-infoflow-mix | https://github.com/ossiai/jianying-infoflow-mix | MIT | 信息流混剪脚本，含素材处理、口播字幕、BGM、剪映草稿生成 | 适合“生成剪映草稿”的路线，逻辑可参考；若不依赖剪映则只复用部分脚本 |
| FrameShift | https://github.com/gaurox/FrameShift | GPL-3.0 | Windows 本地 FFmpeg GUI，已有 Join Videos、拖拽、批量、进度、取消、唯一命名 | 可直接使用或二次开发；注意 GPL 分发约束 |
| FFmpeg Batch AV Converter | https://github.com/eibol/ffmpeg_batch | GPL（以仓库 LICENSE 为准） | 成熟 FFmpeg GUI，支持批量、拼接、多格式 | 功能较重，适合参考，不建议直接作为极简产品内核 |
| PhotonFrame-VideoKit | https://github.com/DavidDirnberger/PhotonFrame-VideoKit | 需确认仓库 LICENSE | FFmpeg 命令行套件，包含 merge/concatenate/add streams | 适合抽取拼接命令逻辑 |
| YoutubeAutomation | https://github.com/letapicode/YoutubeAutomation | 需确认仓库 LICENSE | Tauri GUI + FFmpeg + 可选 intro/outro 的跨平台架构模板 | 可作为 GUI + FFmpeg 架构参考 |
| RxFFmpeg | https://github.com/microshow/RxFFmpeg | 需确认仓库 LICENSE | Android 端视频编辑框架，支持片头片尾、拼接、转码 | 若后续做手机版可参考 |

### 3.4 推荐复用路线

考虑你的需求和开发成本，建议采用以下优先级：

**首选：轻量 GUI + FFmpeg，直接复用 `video-merger-cli` 的拼接逻辑。**

- 核心命令已经覆盖“片头、片尾、批量中间素材”；
- 我们只需在上面加一个极简桌面界面；
- 开发量最小，最快 1–2 周可出可用原型。

**次选：直接基于 `FrameShift` 做定制。**

- 它已经有 Windows GUI、拖拽、批量、进度、唯一命名和 Join Videos；
- 但如果你的产品最终要闭源分发，需要接受 GPL 的传染性要求；
- 如果只是内部自用或愿意开源，则非常划算。

**不建议：从零重写 FFmpeg 封装。**

- 视频参数探测、无损拼接、异常处理都有现成实现，重复造轮子价值低。

## 4. 产品定位

- **目标用户**：信息流广告投放人员、短视频代运营、电商素材制作、自媒体矩阵号运营、需要批量套片头片尾的个人或小团队。
- **产品形态**：Windows 本地桌面应用，后续可扩展为跨平台版本。
- **核心差异点**：
  1. 只需选一个片头、一个片尾；
  2. 中间素材支持拖入多个文件或整个文件夹；
  3. 一键生成多条成片；
  4. 本地处理，素材不上传；
  5. 尽量做到无损快速拼接，必要时自动统一参数。

## 5. 功能需求

### 5.1 MVP 核心功能，P0

1. **片头选择**：选择一个视频或图片文件作为固定开头。
2. **片尾选择**：选择一个视频或图片文件作为固定结尾。
3. **中间素材选择**：支持多选文件，或拖入一个文件夹批量读取。
4. **一键拼接**：按“片头 + 中间素材 + 片尾”的顺序，为每个中间素材生成一条独立成片。
5. **输出设置**：选择输出目录，自动生成唯一文件名，避免覆盖。
6. **处理进度**：显示当前进度、成功/失败数量。
7. **结果预览**：支持直接打开输出目录，或双击查看成片。

### 5.2 重要体验功能，P1

1. 支持拖拽添加文件/文件夹。
2. 支持重排中间素材顺序。
3. 支持单条拼接模式：只拼接片头 + 片尾，不含中间素材。
4. 支持常见视频格式：MP4、MOV、MKV 等；常见图片格式：JPG、PNG 等。
5. 自动处理分辨率、帧率、音频采样率不一致的问题。
6. 拼接失败时给出明确原因，不中断整批任务。
7. 记住上次使用的片头、片尾和输出目录。

### 5.3 进阶功能，P2

1. 自定义输出命名规则，例如：`{中间文件名}_{序号}_{日期}`。
2. 支持给图片中间素材设置展示时长。
3. 支持转场、淡入淡出、片头片尾音量调整。
4. 支持批量加水印、文字贴片。
5. 支持 9:16、16:9、1:1 等常见画幅适配。
6. 支持导出预设，方便团队共享统一规范。
7. 支持在片头、片尾与中间素材之间加入固定间隔或黑场。

## 6. 非功能需求

- **性能**：同编码参数下尽量使用流复制，避免无意义二次编码；100 条 1080P 素材的处理时间应接近存储读写速度，而非每条都完整转码。
- **稳定性**：单条素材失败不影响其他素材继续生成。
- **隐私**：所有文件本地处理，不上传服务器。
- **兼容性**：首期支持 Windows 10/11 64 位。
- **易用性**：新用户无需教程，打开后按“选片头、选片尾、选素材、点击生成”四步即可完成。

## 7. 技术方案

### 7.1 推荐技术栈

| 模块 | 方案 | 说明 |
| --- | --- | --- |
| 桌面框架 | Electron + React / Vue，或 Tauri + React/Vue | Tauri 体积更小，Electron 生态更成熟 |
| 媒体处理 | FFmpeg | 行业标准，拼接、转码、格式统一能力强 |
| 后端逻辑 | Node.js / Rust / Python | 负责任务队列、参数探测、命令生成 |
| 打包 | electron-builder 或 Tauri CLI | 输出 Windows 安装包或绿色版 |
| 配置存储 | JSON 本地文件 | 记录用户上次选择，无需数据库 |

若你更熟悉 Python，也可采用 **Python + PySide6 + FFmpeg**，开发速度更快，但打包体积和启动体验稍弱。

### 7.2 核心拼接策略

优先采用 FFmpeg 的流复制方式：

1. 使用 `ffprobe` 探测片头、片尾和中间素材的分辨率、帧率、编码格式、像素格式、音频参数。
2. 如果所有素材参数一致，使用 `concat` 无损拼接，速度快、画质不损失。
3. 如果参数不一致，先统一为“中间素材优先”的目标参数，再使用 filter 或 concat 进行转码拼接。
4. 每个中间素材单独生成一个成片文件，任务队列串行或有限并行执行。
5. 输出文件名自动去重，避免同一批任务互相覆盖。

其中第 1–3 步可直接参考 `video-merger-cli` 或 `PhotonFrame-VideoKit` 的现有实现，界面层再根据第 8 节设计补上。

### 7.3 系统流程

```text
用户选择片头、片尾、中间素材
          │
          ▼
解析文件列表与参数
          │
          ▼
生成拼接任务队列
          │
          ▼
逐条执行：片头 + 中间素材 + 片尾
          │
          ├── 成功 → 记录并显示成功
          └── 失败 → 记录原因，继续下一条
          │
          ▼
展示结果，支持打开输出目录
```

## 8. UI 设计草案

界面建议保持极简，主页面包含以下区块：

1. 顶部：产品名称和当前版本。
2. 片头区：一个文件选择框，显示文件名、时长、分辨率。
3. 片尾区：一个文件选择框，显示文件名、时长、分辨率。
4. 中间素材区：文件列表，支持多选、拖入、删除、排序。
5. 输出设置区：输出目录和命名规则。
6. 底部：一键生成按钮、进度条、成功/失败统计。
7. 完成后：显示“打开输出目录”和“重新选择素材”按钮。

设计原则：不引入时间轴、不做复杂编辑，只保留“选择、排序、生成、查看结果”。

## 9. 项目里程碑

| 阶段 | 时间估算 | 交付内容 |
| --- | --- | --- |
| 需求确认与原型 | 第 1 周 | 确认功能边界，完成低保真原型 |
| 技术验证 | 第 2 周 | 验证 FFmpeg 无损拼接与转码统一方案 |
| MVP 开发 | 第 3–4 周 | 完成片头/片尾选择、多素材批量拼接、进度与结果 |
| 内测与修复 | 第 5 周 | 处理常见格式兼容、异常素材和崩溃问题 |
| 首发版本 | 第 6 周 | 产出 Windows 安装包，编写简单使用说明 |

若采用 Python + PySide6 原型路线，可将 MVP 开发压缩至 2 周左右；若采用 Tauri + FFmpeg 正式产品路线，首发约 5–6 周。

## 10. 风险与应对

| 风险 | 影响 | 应对措施 |
| --- | --- | --- |
| 素材参数不一致导致拼接失败 | 高 | 建立参数探测与自动转码兜底机制 |
| FFmpeg 打包和授权问题 | 中 | 使用合规 FFmpeg 构建，确认 LGPL/GPL 影响 |
| 批量任务长时间运行卡顿 | 中 | 任务队列异步执行，界面不阻塞 |
| 功能被大厂通用工具替代 | 中 | 聚焦极简、本地、批量三个差异点 |
| 需求理解偏差 | 高 | 先做最小原型，与真实使用场景快速验证 |
| 文件命名与覆盖风险 | 高 | 自动生成唯一文件名，提供预览后生成 |

## 11. 成功指标

- 新用户从打开软件到生成第一批成片的时间不超过 1 分钟。
- 100 条参数一致的素材批量拼接成功率 ≥ 99%。
- 与手动在通用剪辑软件中操作相比，完成同一批任务的操作次数减少 80% 以上。
- 素材本地处理，除 FFmpeg 本地调用外无网络上传行为。

## 12. 下一步建议

1. 建议先采用“轻量 GUI + FFmpeg”路线，直接复用 `video-merger-cli` 的片头片尾批量拼接逻辑，不要从零写 FFmpeg 封装。
2. 用最简单的方式做第一个可运行原型，验证“选片头、选片尾、选多个素材、一键生成”的核心体验。
3. 原型确认后，再补齐命名规则、格式兼容、进度显示和打包发布。
4. 如果后续要发布为闭源商业软件，优先选择 MIT/Apache 等宽松许可证的代码；若复用 FrameShift 或 FFmpeg Batch 这类 GPL 代码，需要评估开源义务。

仍需确认两个问题：

   - 中间素材是否一定会存在，还是有时只需要“片头 + 片尾”？
   - 你需要处理的是视频素材，还是图片 + 音频混合素材？

## 13. 参考资料

- 精易论坛：超级混剪-N片段组合视频混剪，https://att.ijingyi.com/forum.php?mod=viewthread&action=printable&tid=14836406
- npm：video-merger-cli，https://www.npmjs.com/package/@yeah126139163/video-merger-cli
- 驭影批量视频处理工具介绍，https://cps.dazdata.com/topic/312/
- 汇帮视频处理大师，https://www.onlinedown.net/soft/20270632.htm
