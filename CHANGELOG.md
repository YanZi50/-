# 更新日志

> 每次 Git 提交都必须在本文档顶部新增对应说明，内容至少包含：
> 更新了什么、改动了什么、作用是什么、修复了什么、优化了什么。

## 2026-09-14（R44：基础成片显示素材组合上限）

- **基础成片改为显示「所选素材最多可拼条数」**：去重后 开头数×结尾数；固定开头=去重后结尾素材数；固定结尾=去重后开头素材数；固定头尾=1。与引擎组合逻辑完全一致。
- 基础成片数值下方小字显示「素材上限 · 请求 N 条」作对照；预期生成 = 素材上限 × 版本；实际生成 = 引擎最终出片数。三层一目了然：素材上限 → 预期 → 实际。
- 后端 `/api/health_check` 新增 `max_combos` 字段（固定/未固定四场景均验证：6/3/2/1）。
- 验证：浏览器固定开头 + 请求 141 → 面板「基础成片 3 条 · 素材上限 · 请求 141 条 · 预期 3 · 实际 3」。

## 2026-09-14（R43：固定素材时实际条数三处一致 + 组合不足必提示）

- **修复「实际生成 10 条 / 结果 5 条」矛盾**（固定开头 + 差异化关闭场景）：
  - 根因：引擎组合生成对「固定开头/固定结尾」分支**无条件按去重后的唯一素材数出片**（组合不足不重复凑满，也不看去重开关）；而体检计数和任务进度总数未特判固定分支，按去重开关算（关去重=请求数）→ 显示 10、实际 5、无提示。
  - 修复：`_actual_total` 与生成端完全一致——固定头尾=1；固定头/固定尾=min(请求数, 去重后另一端素材数)；未固定时去重开=min(请求,组合)、去重关=请求。`run_one_batch` 进度/日志总数统一改用 `_actual_total`。
  - 提示补全：固定素材导致组合不足时，**无论去重开关**都在体检中警告，文案「固定素材后仅 N 种不同组合，实际将生成 M 条（不重复出片，避免平台判重）」。
- 验证（测试素材：固定头 + 去重关 + 请求 10 + 结尾 6 个文件去重后 5）：
  - 体检 actual_count=3/5、生成数量 warn、start total=5、日志「任务开始，共 5 条」、进度 5/5、实际输出 5 个 mp4、结果与显示完全一致。
  - 未固定素材回归：去重关 10 条、去重开组合不足时 actual=组合数（原逻辑不变）。

## 2026-09-14（R42：预览视频可拖动进度 + 体检面板出片结构完善）

- **预览视频支持拖动进度**：文件服务原为整体 200 响应（不支持 Range），浏览器无法 seek。现 `_send_file` 支持 Range 请求（206 + Content-Range + Accept-Ranges），预览/结果视频均可拖动进度条，静态资源下载同时获得断点能力。实测 curl Range 返回 206，浏览器 video seek 成功（duration 3.4s 跳至 1.7s）。
- **体检面板出片结构**：按「基础成片 → 差异化 → 预期生成 → 实际生成 → 预计生成时间」展示：
  - 基础成片 = 请求条数；预期生成 = 条数 × 版本数；实际生成 = 后端真实最终出片数（组合不足时少于预期，与生成端完全一致）。
  - 修复「实际生成」显示 bug：原用 count×versions（预期值），现用后端 actual_count。
- **修复去重开关不同步**：`dedupe_enabled` 默认 true 但 UI 由 `dedupe_level` 驱动，导致后端按"开启"判定而页面显示"未开启"（实际生成数虚低）。collectConfig 现显式同步 `dedupe_enabled = dedupe_level !== 'off'`。
- **自动预检改显式触发**：步骤导航/上一步/下一步统一走 `goStep(n)`，进入生成页即触发预检（原 watch 方案在部分场景未生效，现双保险）。
- 验证：去重关 基础10/预期10/实际10；去重开 基础10/预期10/实际2；自动预检在切页时发出体检请求并渲染面板。

## 2026-09-14（R41：预览仅按钮触发 + 删预检按钮 + 出片条数全链路一致）

- **预览只在点「生成预览」时生成/显示**：原逻辑是"任务总数为 1 时完成即自动显示预览"，导致普通生成 1 条也会弹出预览。改为仅「生成预览」按钮触发的任务完成后显示预览区；普通任务（含 count=1）完成后不显示。实测：点预览→显示；普通生成→不显示。
- **删除「预检素材」按钮**：功能不变——进入「生成与结果」页自动预检、开始生成前自动预检、面板内「重新体检」均保留。
- **实际出片条数全链路一致**：预检提示的条数 = 开始生成显示的条数 = 任务日志条数 = 最终结果条数。
  - `/api/start` 返回实际将生成的条数，前端进度用真实条数（原显示虚高的请求数）；
  - 任务日志/进度总数按实际生成条数计算（去重开组合不足时按组合数，固定头尾=1，去重关=请求数）；
  - 组合数计算改为与生成端一致（素材先指纹去重再计数，重复拷贝不再重复计入）；
  - 预计生成时间按实际条数估算（不再虚高）。
- 验证：10 条请求、素材去重后 2 种组合 → 预检 actual_count=2、start total=2、日志"共 2 条"、结果 success=2，四处完全一致。

## 2026-09-14（R40：启动不恢复固定素材 + 组合不足提示修正）

- **素材库默认不固定**：此前启动时会从上次保存的配置恢复固定素材（表现为"默认固定"）。现改为启动/载入默认配置时一律不恢复固定（固定素材清空），由用户手动点选固定；从历史任务/模板恢复时仍保留固定状态。
- **组合不足提示修正**：原提示"超出部分按可用组合生成（不重复出片）"在**去重关闭**时是误导（实际会随机重复凑满）。现区分：去重开启 → 明确提示"只生成 N 条不重复（不会重复出片）；关闭去重可凑满 X 条（可能重复）"；去重关闭 → 不再警告。
- 验证：保存含固定素材的配置后刷新，固定状态为空（"未固定（随机抽取）"）；去重开/关两种提示文案均正确；清理测试时覆盖的本地配置。

## 2026-09-14（R39：进入生成页自动预检 + 固定素材提示 + 体检提醒悬停详情）

- **进入「生成与结果」自动预检**：切换到该页时若有素材路径且非运行中，自动静默执行体检（不弹 toast，面板直接展示结果）；手动点「预检素材」仍保留完整提示。
- **固定素材提示**：体检面板新增「固定素材」项，逐库说明当前状态——如 `开头：已固定 23.mp4；结尾：未固定（随机抽取）；池1：未勾选（随机 2 条）；背景音乐：未使用`，一眼看清哪些库固定了、哪些是随机。
- **提醒悬停详情**：「通过 · N 项提醒」徽标鼠标悬停显示每条提醒的具体原因（生成数量不足、磁盘建议等），样式化 tooltip。
- 验证：切页自动预检生效（面板自动出现）；固定素材项正确输出；warn 徽标 data-tip 内容正确；无 JS 报错。

## 2026-09-14（R38：中间素材池「展开」按钮固定右端）

- 修复：中间素材池的「展开/收起」按钮此前依赖「移除」按钮的 `margin-left:auto` 才贴右，单池时「移除」隐藏导致按钮跑到左边。现改为展开按钮自带 `margin-left:auto`，无论池数量多少都固定在池标题行最右（与开头/结尾素材库一致，位于「选择」按钮正上方）。
- 验证：单池与两池实测展开按钮均右端对齐（右缘间隙 0、为行内最后一个元素）；无 JS 报错。

## 2026-09-14（R37：滑块防跳动 + 疑似重复提示修正）

- **出片数量滑块不跳动**：`.range-val` 固定宽度 58px（原 min-width 42px），数字从"1 条"变"200 条"不再挤压滑块、不再引起布局跳动。实测 1/50/200 时数字宽与滑块宽完全恒定。
- **疑似重复提示不再残留**：原问题——新任务开始时不清理，刷新页面还会拉上次任务的相似对。现修复：`/api/start` 与前端 `startJob` 都在任务开始瞬间清空旧相似对；只有本次任务完成且确有重复时才显示，新任务开始即消失。
- **疑似重复限制 5 条/页**：超过 5 对出现翻页（标题行内 ‹ 1/2 ›），新任务完成自动回第 1 页。
- 验证：新任务开始后 /api/similar 返回 0 对；1 对正常显示且无翻页条；无 JS 报错。

## 2026-09-13（R36：预计生成时间 + 结果/历史列表约束）

- **预计生成时间**：预检面板新增"预计生成时间"格（如 `约 13 秒`）。估算逻辑：优先用最近一次任务实测速度（条/秒含并发），无历史时按编码方式保守估算（显卡 ~2.5s/条、CPU ~12s/条）× 条数 ÷ 并发 + 缓冲。
- **生成结果翻页**：结果最多每页显示 5 条，超过 5 条出现翻页条（上一页/下一页 + 页码，表头和底部各一组），新任务完成自动回第 1 页；移除原"展开全部"。
- **历史任务滚动**：任务历史最多显示约 5 条高度，超出在区域内滚动查看（细滚动条），不再平铺撑高页面。
- 验证：6 条结果实测 2 页（第 1 页 5 条/第 2 页 1 条）；预检实测返回 `约 13 秒`（10 条 nvenc）；历史区 5 条内滚动；无 JS 报错。

## 2026-09-13（R35：疑似重复提示布局优化）

- **重复率一眼可读**：疑似重复对从"一行全挤"改为"每对一行卡片式"——左侧两个文件名（超长自动截断，悬浮显示全名），右侧独立圆角徽标显示重复率（如 `100%`，warn 底色、加粗），不再被长文件名挤压。
- 验证：长文件名（如 `长文件名测试_001_h2_t1.mp4`）实测布局正确，徽标独立显示、文件名截断、无 JS 报错。

## 2026-09-13（R34：素材库两排显示 + 内部滚动）

- **素材库展开改为"最多两排 + 内部滚动"**：开头/结尾素材库、每个中间素材池、背景音乐四个区域统一改。展开后最多显示两排素材，超出部分在区域内滚动查看（细滚动条），不再撑高页面、不再用"加载更多"按钮。
- **自适应精确两排**：两排高度由 JS 按实时卡片高度测量（cardH×2+gap），任意窗口宽度下都恰好两排，不写死像素；素材少于两排时不会压缩。
- 素材全量渲染 + 图片懒加载，几百条素材展开流畅；窗口缩放时自动重新适配。
- 验证：3 条素材实测容器 max-height=598px（=2×293+12）、overflow-y:auto、全量渲染、超出可滚动；无 JS 报错。

## 2026-09-13（R33：导出投放清单 CSV）

- **投放清单导出**：生成结果区新增"导出投放清单（CSV）"按钮，一键导出本批成片清单（序号、成片文件名、完整路径、开头/中间/结尾素材），BOM UTF-8 编码，Excel 直接打开不乱码；文件名带日期时间（如 `投放清单_20260913_1425.csv`），方便投放登记与归档。
- 数据来自本批任务快照（成功项），无后端改动，前端本地生成下载。
- 验证：浏览器渲染正常（无 JS 报错），导出逻辑使用真实成功项字段。

## 2026-09-13（R32：多批次任务队列）

- **任务队列**：生成页新增"任务队列"卡片，可预先排队多批不同配置（批次名称 + 完整参数快照），当前任务完成后自动依次执行，适合挂机连跑。
- 队列项显示：批次名称 + 出片数×去重版本 + 分辨率 + 去重开关 + 时长模式；支持移除/清空。
- 语义：加入队列后先"开始生成"会立即跑当前配置，跑完自动接续队列；取消会停止整个队列（队列保留可手动移除）；重启服务后队列清空。
- 验证：3 批（1 条立即 + 队列 2 批 1/2 条）自动顺序执行成功，队列全部消费；回归 38 项全绿。

## 2026-09-13（R30：产物感知哈希查重）

- **生成后自动查重**：任务完成后对本批输出两两比对感知哈希指纹（每视频均匀抽 3 帧 → dHash 64bit/帧 → 拼接比对），相似度 ≥88% 判定疑似重复。
- **结果页提示**：生成结果下方展示"疑似重复输出"告警（如 `output_002_h2_t1.mp4 ≈ output_001_h1_t1.mp4 100%`），帮助发现同批里自己撞自己的内容；刷新页面后仍可查看上次任务结果。
- **实测**：去重关闭时相同内容 100% 检出；全维度深度差异化下同源素材输出仍 94% 相似——这准确反映平台判重视角（同源素材即使深度差异化也可能不够，提示需换素材或更强调差异化）。
- 验证：相同/差异化两组端到端查重均正确；回归 38 项全绿。

## 2026-09-13（R29：深度差异化——变速/镜像/噪点/音调 + 输出规格修复）

- **深度差异化（去重页新增）**：在原有画面微调/片段/音频三维度基础上，新增 4 个更强扰动维度，全部独立开关 + 三档强度（低 ±1% / 中 ±2% / 高 ±3%）：
  - 变速不变调：整体速度 ±1-3%，画面与声音同步（setpts+atempo，变速后强制 fps=30 保持 CFR）
  - 水平镜像：hflip 翻转，打破画面指纹
  - 轻噪点：叠加胶片颗粒（3-16 级强度可调）
  - 音调微移：声音升/降调 ≤3%，时长不变（asetrate+aresample+atempo）
- **修复（顺带）**：① 2 段拼接且差异化开启时不再走流复制（此前 visual 差异在只有头尾时静默失效）；② visual 随机裁切缩放浮点误差导致输出 1078x1918 而非 1080x1920——改为按目标分辨率精确裁剪还原（SAR 也恢复正常 1:1）。
- 验证：深度差异化全维度端到端生成成功（2 条互异、时长随变速变化、音轨正常）；输出精确 1080x1920/SAR 1:1/DAR 9:16；回归 38 项全绿。
- 提示：深度差异化扰动更强，平台判重严重时再开启；强度中档推荐。

## 2026-09-13（R28：NVENC 显卡加速转码）

- **编码加速选项**：参数配置页新增「编码加速」（自动检测 / 显卡加速 / 仅CPU），贯穿归一化、拼接、转场、BGM、水印、字幕全部转码环节。
- **加速原理**：自动检测时若 ffmpeg 支持 h264_nvenc 且显卡可用（NVIDIA），用 NVENC 硬件编码（恒定质量 CQ 模式，qp 从原 crf 映射）；否则回退 CPU x264，不影响任何机器。
- **实测提速**：60s 素材 1080×1920 生成，CPU 12.5s → NVENC 2s，**约 6 倍提速**；并发多路时收益更大（RTX 3060 实测）。
- 参数快照面板同步显示编码加速状态。
- 验证：CPU/NVENC 双模式端到端生成，输出分辨率/时长/音轨正确；回归测试 38 项全绿。

## 2026-09-13（R27：清理冗余 + 新图标 + 便携版重打包）

- **清理**：删除归一化缓存 cache/、日志 logs/、__pycache__、旧打包产物（dist/build）；保留用户数据（history/ 任务历史、templates/ 模板）。
- **新图标**：生成品牌图标（深蓝紫渐变圆角底 + 胶片四格 + 播放三角，3D 质感），多尺寸 app.ico 嵌入 exe；网页 favicon 同步更新（web/app.ico + index.html link）。
- **便携版重打包**：PyInstaller onedir + 内置 ffmpeg（bin\ffmpeg.exe）+ 新图标；已验证 exe 独立运行、生成视频正常；打包脚本已加 --icon 参数。
- 产物：`信息流素材一键拼接-便携版.zip`（78.7MB，含全部运行时组件，解压后双击 exe 即用，自动打开浏览器）。

## 2026-09-13（R26：生成中参数保护——修改提示 + 本次任务参数快照）

- **生成中修改提示**：任务运行期间点击素材库/参数/去重页任意控件，右上角 toast「生成中：当前修改将在下次任务生效，本次任务参数已锁定」（2.5s 防抖，不阻断操作）。
- **本次生成参数快照**：点「开始生成」时参数留档，「生成与结果」页新增可折叠「本次生成参数」面板（出片/并发/分辨率/时长/适配/转场/BGM/水印/去重/命名/素材路径/输出目录共 14 项）；刷新页面后从后端 `status.last_config` 自动恢复（断点续跑/重试也沿用原任务快照，保证核对一致）。
- 机制说明：任务启动时后端已将参数快照为 JobConfig，生成中改界面不影响进行中的任务（重试/续跑均用原快照）。
- 顺带修正：体检「生成数量」提示与实际引擎行为一致（组合不足时**按可用组合生成，不重复出片**，原文案误写为"将重复组合"）。
- 验证：慢任务端到端——运行中点击参数区 toast 正常弹出（含防抖）；快照面板 14 项完整、折叠正常；后端 last_config 返回/恢复正常；回归测试 38 项全绿。

## 2026-09-13（R25：预检产出概览）

- 点击「预检素材」后，体检面板顶部新增**本次产出概览**（4 个统计块）：
  - **基础出片**：参数配置的出片数量（如 10 条）
  - **差异化**：未开启 / 已开启 · 强度（轻度/深度）
  - **每条版本**：一键多版本的版本数（关闭差异化时隐藏）
  - **实际生成**：出片 × 版本 = 总条数（如 10 × 5 = 50 条），去重开启时高亮
- 后端体检「生成数量」条目同步联动：`请求 N 条 × M 版 = 共 T 条`，组合数不足时仍提示重复组合。
- 验证：浏览器端到端（出片 10 × 深度 5 版 → 概览显示 50 条；后端条目同步）；回归测试 38 项全绿。

## 2026-09-13（R24：修复水印/素材库中文路径乱码）

- 根因：Windows 下 PowerShell 对话框返回值经控制台输出（GBK 代码页），Python 侧按 UTF-8 解码 → 中文路径乱码（水印选择；素材库选文件夹有同样隐患）。
- 修复：对话框改为 `.ps1 文件方式`（UTF-8 BOM 写入，保证脚本中文正确读取）+ 结果 **Base64(UTF-8) 传输**（纯 ASCII 不受控制台代码页影响），Python 侧解码回真实路径。`run_folder_dialog` 与 `run_file_dialog` 统一走 `_run_ps_file`。
- 验证：中文路径「D:\测试目录\水印\logo透明.png」无损往返；取消对话框返回空；回归测试 38 项全绿。

## 2026-09-13（R23：修复水印无法选择路径）

- 根因：R18 移除素材库上传功能时删除了 `/api/upload`、`/api/upload_path` 后端接口，但水印选择仍依赖该接口 → 404 导致无法添加。
- 修复：新增服务端文件选择对话框 `/api/select_watermark`（PowerShell OpenFileDialog，过滤 PNG/JPG/WebP，与素材库目录选择同构），前端「选择文件」改为直接引用本地水印图片路径——**不拷贝副本**，与 R18「连接本地路径」理念一致；水印路径自动加入后端白名单。
- 验证：浏览器确认水印区「选择文件」按钮渲染、无 JS 错误；回归测试 38 项全绿。

## 2026-09-13（R22：去重界面美化——简单易懂）

- 去重页（步骤 3）视觉重构：
  - **顶部引导卡**：主色渐变底 + 一句话说明（平台查重原理与差异化作用）
  - **差异化强度**：三档大卡片按钮（关闭/轻度/深度），每档带圆点指示与适用场景说明，选中态主色高亮
  - **差异化维度**：三个选项卡（画面微调/片段差异化/音频差异化），勾选态绿色高亮，关闭差异化时置灰禁用
  - **一键多版本**：版本数字大号展示（如「3 版」）+ 实时计算总条数（出片 × 版本 = 实际生成 N 条）
  - 顶部标题/描述随步骤联动（新增第 3 步「去重差异化」文案，原生成步骤顺延为第 4 步）
- 验证：浏览器结构检查（三档选中态/维度禁用态/总数计算）+ 截图视觉确认（浅色主题布局正常）。

## 2026-09-13（R21：去重差异化——独立第 3 步 + 内容变体生成）

- 界面：**左侧栏由 3 步改为 4 步**（1 素材库 / 2 参数配置 / **3 去重** / 4 生成与结果），去重单开一页，位于参数配置与生成之间。
- 去重页功能（三档强度 + 三勾选项 + 一键多版本）：
  - **差异化强度**：关闭 / 轻度 / 深度（深度含片段级差异化）
  - **画面微调**：亮度/对比度/饱和度 ±5–10% 随机 + 随机裁切缩放 3–8%（并入转码链，零额外转码）
  - **片段差异化**：**随机入点偏移**（-ss 输入级裁剪 0.05–0.45s，同一素材每次用不同片段）+ **深度档随机转场**（类型+时长随机，打破剪辑序列指纹）
  - **音频差异化**：**BGM 随机起播位置**（atrim 偏移，保证剩余长度覆盖成片）
  - **一键多版本**：每条组合出 N 个差异化版本（N=1–5，各版本 seed 不同，命名序号自动区分）
- 技术要点：差异化参数由 `dedupe_delta(level, options, seed)` 确定性生成（同 seed 可复现）；入点偏移用输入级 `-ss` 而非 trim 滤镜（ffmpeg 7.1 trim+xfade 报 -22）；crop 尺寸偶数化（yuv420p 要求）。
- 验证：off 模式行为不变；deep×2 版产出两版首帧不同（差异化生效）；BGM 起播偏移日志确认；浏览器 4 步导航与去重页交互正常；回归测试 38 项全绿。
- 说明：本功能做正常内容变体生成（同一内容合理出多版本），不包含闪烁帧/隐藏水印等规避平台审核的对抗手段。

## 2026-09-12（R20：去重体系升级——素材指纹去重 + 组合防重复 + 中间素材排除）

- 新增：**素材内容指纹去重**——`media_fingerprint`（文件大小 + 头尾各 1MB 的 SHA256）+ `dedupe_by_fp`。同一视频换文件名/放不同文件夹也会被识别为重复：
  - 生成时开头/结尾/中间/背景音乐按指纹自动去重（保留每个内容的第一份）
  - 素材库扫描（`/api/scan`）返回指纹，前端同内容非首个卡片标红色「重复」角标（head/tail/中间池/BGM 均生效）
- 修复：**组合数不足时不再重复出片**——原逻辑 count 大于可用组合数时会循环复用同一批组合（产生重复成片）；现返回全部可用组合并在日志提示「素材组合数不足…不会重复出片」。
- 新增：**中间素材排除重复**——随机抽取中间素材时排除与开头/结尾同名同路径的素材，多池时已选素材也不会重复出现在同一条成片里。
- 新增：**文件名模板变量 `{时间}` `{随机4位}`**（与已有 `{序号}{开头}{结尾}{中间}{日期}` 组合使用，如 `我的视频_{日期}_{时间}_{随机4位}`，前端提示已更新）。
- 验证：单元验证（指纹去重/组合补洞/中间排除/变量渲染）+ 端到端（head 含重复内容时 count=3 只生成 2 条可用组合）+ 回归测试 38 项全绿 + 浏览器重复徽标显示正常。
- 测试更新：`test_middle_random_count` 因去重行为变化（组合不足不再循环复用）调整素材组合数，测试目的不变。

## 2026-09-12（R19：音量调节——原声音量 + BGM 音量滑块）

- 新增：**原声音量滑块**（0–200%，默认 100%）——调整成片素材原声/口播音量大小。有 BGM 时作用于混音链原声轨，无 BGM 时单独重编码音频（视频流复制，速度快）。
- 新增：**BGM 音量滑块**（0–200%，默认 20%）——背景音乐相对原声的音量。原来该参数在后端写死 0.2 且前端不可调，本次暴露为滑块，三种 BGM 模式均生效。
- 说明：与「统一音量（响度归一）」不冲突——归一化管响度拉平（各素材音量一致），音量滑块管整体大小，串联执行（先归一化、后按比例缩放）。
- 前端：底部「原声音量 / BGM 音量」两个滑块（原 BGM 设置区内旧的 0–100% 滑块已移除）；后端 `_make_config` 读取 `audio_volume`（0–2.0）、`bgm_volume` 上限扩至 2.0。
- 验证：volumedetect 实测——原声 100%→50% 输出差 6.0dB（理论 6.02dB）；BGM 20%→100% 整体响度提升 14dB；回归测试 38 项全绿；滑块 UI 可操作（0–200%、显示百分比）。

## 2026-09-12（R18：移除素材上传拷贝，素材库只留「选择」本地文件夹）

- 调整：**取消选择开头/结尾素材后自动填充输出目录**——输出目录完全由用户手动选择。
- 移除：**素材库「上传」按钮**（开头/结尾/中间素材池/背景音乐）——上传会拷贝一份文件副本到程序 uploads 目录（占双份磁盘、素材更新不同步）。现只保留「选择」按钮：连接本地文件夹、引用原路径、零拷贝；界面文案同步改为「选择素材/音乐文件夹」。
- 清理：删除后端 `/api/upload`、`/api/upload_path` 端点及 `_upload_folder_for_kind`、`_upload_file` 方法（前端已无调用）。
- 验证：页面无「上传」按钮（仅剩选择/随机）；选择开头后输出目录保持为空；upload 端点返回 404；JS/Python 语法检查通过。

## 2026-09-12（R17：生成结果表格折叠）

- 新增：**生成结果表格折叠**——成片数量多时不再整页拉满：
  - 默认只显示前 5 条结果，底部提示「还有 N 条结果，点击展开全部」；
  - 表头右侧「展开全部 ▾ / 收起 ▲」一键切换；
  - 展开后显示全部结果行，再次点击收起回到前 5 条。
- 验证：30 条结果实测——默认 5 行 + 提示 25 条，展开后 30 行，收起回到 5 行；JS 语法检查通过。

## 2026-09-12（R16：修复素材库 BGM 卡片误标「损坏」）

- 修复：素材库中 BGM 音频文件左上角显示「损坏」但实际可用——R15 只修复了体检（precheck）路径，素材库扫描路径仍要求视频流：
  - 后端 `/api/material_detail` 支持 `kind=bgm` 参数，纯音频按音频标准探测（require_video=False）；
  - 前端 `enrichMaterials` 接收素材类型，BGM 判定改为「有音频流即正常」，视频素材判定不变。
- 验证：用户真实 BGM 目录 `D:\MyFolder\拼接视频素材\bgm` 的 6 个 MP3 探测全部 ok（has_audio ✓，时长正常）；浏览器实测 BGM 卡片不再显示「损坏」。

## 2026-09-12（R15：修复 BGM 被误判损坏导致体检未通过）

- 修复：**BGM 文件夹里的正常音频被体检/预检标为「无法读取」**——`probe_media` 的健康判定要求视频流（`has_video`），纯音频文件（BGM）没有视频流 → 全部误报损坏 → 体检未通过（表现为"预检失败"）。
  - `probe_media(path, require_video=False)` 新增参数：音频场景改为「有音频流或视频流且时长正常」即判定健康；
  - 预检（`precheck_materials`）对 BGM 类素材以音频标准探测，错误文案区分「无法读取音频 / 无法读取视频流」；
  - 视频类素材判定逻辑不变。
- 验证：生成 2s 纯音频 WAV → 修复前 ok=False、修复后 ok=True（has_audio、duration 正常）；`/api/health_check` 带 BGM 文件夹实测五项全部通过。

## 2026-09-12（R14：转场可视化预览 + 开始前全局体检）

- 新增：**转场可视化预览**——参数页转场卡内置预览窗口，39 种转场用 CSS 动画实时演示（淡入/滑动/擦除/圆形/缩放/像素/开合/挤压等 16 类动画），无需真实渲染：
  - 固定转场模式：预览跟随下拉选择的转场类型；
  - 随机转场模式：点击转场池中的任一转场即播放该转场的预览；
  - 切换转场时动画自动重放。
- 新增：**开始前全局体检**（`/api/health_check`）——点击「开始生成」时自动执行，任务控制卡内展示体检面板：
  - 素材健康：无法读取的素材逐个列出（阻断）；
  - 输出目录：不存在（提醒将自动创建）/ 不可写（阻断）；
  - 磁盘空间：剩余空间 vs 预估输出体积（按素材时长与分辨率粗估），不足阻断、偏紧提醒；
  - 生成数量：请求条数超过素材可组合数时提醒将出现重复组合；
  - 引擎：FFmpeg 可用性（阻断）。
  - 存在阻断项（error）时任务不启动并提示；面板按 ok/warn/error 分级展示，可一键「重新体检」；原「预检素材」按钮升级为体检。
- 验证：API 实测五项检查全部正确返回；浏览器实测固定模式切换（放大进入/上擦除）与随机模式点击（圆形打开）预览动画与标签正确、切换即时重放；开始生成自动体检通过后任务正常启动。38 个回归测试通过。

## 2026-09-12（R13：左侧栏美化 + 必填校验标红，取消打勾）

- 调整：**取消步骤完成打勾（✓）**——侧栏步骤只显示数字，不再显示勾选标记，页面无完成状态噪音。
- 新增：**必填项校验标红**——必填项仅「输出目录」一项；点击「开始生成」时校验，输出目录为空则：
  - 左侧「素材库」步骤**整项变红**（红字 + 红底 + 右侧红点 + 呼吸提示动画）；
  - 右侧提示「请选择输出目录（左侧「素材库」已标红）」，任务不启动；
  - 填入输出目录后红色**实时消失**（手动输入或点「选择」按钮均可触发）。
- 美化：左侧栏整体精修——hover 微移 + 文字加深、选中态左侧渐变指示条 + 数字发光、错误态红色脉冲动画，深浅主题均适配。
- 验证：浏览器端到端——输出目录留空点开始生成 → 素材库标红 + 提示 + 任务不启动；填入输出目录 → 红色立即消失；再点开始生成 → 任务正常启动。JS 语法检查通过。

## 2026-09-12（R12：自定义命名自动追加序号 + 随机种子一键刷新）

- 修复：**自定义命名只导出 1 条、其余被跳过**——模板不含 `{序号}` 时（如 `我的视频`），所有输出同名，第二条起被"已存在即跳过"逻辑跳过。现改为：模板不含 `{序号}` 时**自动在末尾追加序号**（`我的视频` → `我的视频_001/002/003…`），用户自定义前缀保留，每条都正常导出，且与断点续跑的跳过逻辑兼容。
- 更新：文件名模板输入框下方提示同步说明（"模板不含 {序号} 时自动追加序号，保证每条都导出"）。
- 新增：随机种子旁新增**「随机」按钮**，一键随机刷新种子（0–1e9），方便快速换一批随机组合，无需手动填数。
- 验证：命名单测——含序号模板不变、`我的视频`→`我的视频_002`、空模板回退默认且不重复；端到端 count=3 + 模板`我的视频` → 成功 3/跳过 0，输出 `我的视频_001/002/003.mp4`；浏览器实测随机按钮点击后种子更新 + 输入框同步 + 右侧提示。

## 2026-09-12（R11：断点续跑 + 素材库布局调整）

- 新增：**断点续跑**——服务被强杀/崩溃/重启后，未完成的任务不丢失，可从中断处继续：
  - 任务启动时把完整配置写入磁盘快照（`state/last_task.json`）；任务正常完成后自动清除。
  - 服务重启时自动检测残留快照，`/api/status` 返回 `interrupted`，前端在「生成与结果」页显示横幅并弹出提示：「检测到上次任务中断，可继续生成（已完成输出自动跳过）」。
  - 新增 `/api/resume`（继续生成）与 `/api/discard_interrupted`（放弃）端点；续跑复用 `process_batch` 的 `skip_existing` 机制，**已完成输出自动跳过、不重复生成**。
  - 取消/异常/崩溃均保留快照（便于续跑），仅正常完成清除；新任务开始自动放弃旧中断。
  - 快照路径适配便携版（exe 旁 `state/` 目录）。
- 调整：素材库卡片顺序——**背景音乐移到中间素材池下方**（原来在结尾素材库之后）：开头素材库 → 结尾素材库 → 中间素材池 → 背景音乐 → 输出目录。
- 验证：端到端——50 条任务跑到 22 条时强杀服务 → 快照残留 → 重启后 status 返回 interrupted → 续跑 → 最终 成功 28 + 跳过 22 = 50 个输出全部存在、快照自动清除；前端横幅/toast/「继续生成」「放弃」按钮实测正常；全套 38 个测试通过。

## 2026-09-12（R10：新增画面适配模式——黑边 / 模糊填充 / 智能裁剪）

- 新增：素材比例与目标分辨率不一致时的画面适配方式，参数页新增"画面适配"选项（3 种）：
  - **黑边（默认 fit）**：等比缩放 + 黑边补齐（letterbox），即原行为，默认不改变。
  - **模糊填充（blur）**：背景铺满 + 高斯模糊，前景内容等比居中（信息流标准观感，无黑边）。
  - **智能裁剪（crop）**：等比放大铺满画面、居中裁剪多余边缘（无黑边，可能裁掉部分内容）。
- 改动：
  - `video_engine.py`：`_filter_scale_pad` 按模式生成滤镜链（blur 走 `-filter_complex` 分路 split/overlay）；`normalize_clip` 新增 `fit_mode` 参数并透传；`JobConfig` 新增 `fit_mode` 字段；归一化缓存键加入模式（fit 省略后缀，**旧缓存继续命中**，blur/crop 独立缓存）。
  - `web_app.py`：`_make_config` 解析 `fit_mode`（默认 `fit`，旧配置/模板完全兼容）。
  - 前端：参数页新增"画面适配"分段控件（黑边/模糊填充/智能裁剪）+ 说明文案；`params.fit_mode` 随模板/预设一并保存与恢复。
- 验证：三种模式归一化实测——同素材（320×480）输出 1080×1920：fit 四角纯黑（黑边）、blur/crop 四角为内容色（无黑边），时长/尺寸/音频正常；浏览器端到端 blur 任务 10 条全部成功，成片 3.95s、1080×1920、四角无黑边；全套 38 个测试通过。

## 2026-09-12（R9：提示改为右侧悬浮，消除页面跳动；页面整体美化）

- 更新：所有即时提示（"已固定开头""已取消固定"、任务状态、错误提醒等）从顶部文档流内改为**右侧悬浮 toast**：
  - `web/index.html`：提示节点移出 `.content` 文档流，置于 `#app` 顶层（`position: fixed`，不占布局空间）；原 topbar 下方文档流内的提示块已删除。
  - `web/style.css`：新增 `.msg-toast` 样式——右上角悬浮（top 18px / right 18px / z-index 9999）、滑入动画、类型圆点（info/success/warn/error 四色）、6 秒自动消失。
  - 效果：点击固定/取消固定时**页面不再上下跳动**（topbar 位置恒定），提示浮于内容之上不遮挡操作。
  - 保留：步骤 3 预检失败的 `warn` 列表仍为文档流内联提示（内容型警示，需随页面滚动查看）。
- 优化：页面整体美化（`web/style.css`）：
  - 品牌 Logo、选中角标、进度条渐变由紫色（#7c5cff）改为蓝青渐变（#3b82f6 → #06b6d4），视觉更清爽。
  - 页面背景增加柔和光晕（`body::before` 双径向渐变，深浅主题自适应）。
  - 卡片 hover 提升（边框 + 阴影）、主按钮改为渐变背景 + 柔光阴影、输入框 hover/focus 增强、主题切换按钮 hover 上浮。
  - 表头大写 + 字距、滚动条 hover 变色、材料卡片固定角标加阴影。
- 验证：`node --check web/app.js` 通过；浏览器实测——点击素材卡片触发 toast 后，提示出现在视口右上角（position fixed、top 18、right 28），topbar 位置在提示出现与消失前后均为 0（不跳动）；深色/浅色双主题视觉确认正常；服务 `/api/status` 200。

## 2026-09-12（R8：清理项目冗余文件，释放约 363MB）

- 清理（均不影响正常使用，运行时自动重建/重新生成）：
  - `cache/`（194MB，归一化+缩略图缓存，其中含 R6 之前已失效的 normv1 旧缓存；下次任务自动重建）
  - `dist/`（131MB，便携版中间产物，便携包 zip 已在项目上级目录交付，可随时解压使用）
  - `build/`（29MB，PyInstaller 中间产物）、`previews/`（8MB，预览图缓存）、`__pycache__/`
  - PyInstaller 生成的 `.spec` 文件（`sppj_debug.spec`、`信息流素材一键拼接.spec`）
- 删除已废弃源码 `portable_app.py`（R7 起自动开浏览器逻辑已内置 web_app.py，该入口无任何引用）。
- 保留：`history/`（任务历史）、`logs/`（运行日志）、`uploads/`（用户上传素材）及全部源码/文档/测试。
- 验证：py_compile 全过；浏览器端到端任务成功（2 段成片 3.47s 符合预期），cache 自动重建。全套 38 个测试不受影响（未涉及引擎逻辑）。

## 2026-09-12（R7：便携版打包，任意 Windows 电脑免安装使用）

- 新增：PyInstaller 打包为免安装版（`dist\信息流素材一键拼接\`），双击 exe 即用，自动打开浏览器。
- 新增：`打包便携版.bat` —— 一键重新打包（PyInstaller + 自动把 FFmpeg 复制到 exe 旁）。
- 引擎调整：`_ffmpeg()` 在打包（frozen）时优先查找 exe 旁 `bin\ffmpeg.exe`（用户数据区）。原因：实测本机环境中 PyInstaller `_internal` 目录内的可执行文件被系统防护拒绝访问/执行（cmd、Python 均 PermissionError/WinError 5），而 exe 旁目录正常；FFmpeg 必须放在 exe 旁才能保证换机可用。
- 服务调整：web_app.py 顶部在打包运行时剔除外部 `PYTHONPATH` 注入（本机存在 `D:\Doubao\...\python-packages`），防止模块从开发机源码/包加载导致：①FFmpeg 引用开发机路径（换机即挂）；②缓存目录错落到项目目录。打包后模块一律从 `_internal` 打包副本加载。
- 服务调整：打包运行时启动后自动打开浏览器（`webbrowser` 延迟 1s），免手动输入地址。
- 新增调试端点 `/api/debug`（只读）：返回 frozen/executable/meipass/web_dir/engine_file/app_root/cache_dir/ffmpeg 运行时路径，便于排查便携版问题。
- 验证：便携版在本机 `dist\` 与模拟换机副本（portable_test）双位置端到端通过：4 段任务成片 6.40s、yuv420p、1080×1920；FFmpeg 与缓存均跟随 exe 目录，不依赖开发机任何路径。全套 38 个自动化测试通过。

## 2026-09-12（R6：修复拼接音画不同步，三个根因）

- 修复：拼接成片音画不同步 / 成片时长明显短于预期 / 成片体积异常偏大。
- 根因与改动：
  1. **链式转场 offset 递推错误（主因）**：`concat_chain` 中第 i 个 xfade 的 offset 用 `sum(durations[:i+1]) - d` 计算，但前 i 段拼接输出已减去 i 个转场重叠，正确应为 `当前输出时长 - d`（递推）。offset 超出输入时长时 ffmpeg 静默截断输出 → 多段成片时长不足、片内音画错位。`concat_three` 的 o2 同样少减一个 d。已改为递推累加（`acc = off + durations[i+1]`）。
  2. **转场 offset 使用原素材标称时长**：`_process_one_combo` 以原素材 probe 时长计算 offset，而实际拼接的是归一化后文件（`-shortest` + AAC 帧对齐会使时长改变，如 2.0s → 1.97s），过渡点与音频 acrossfade 边界错位。改为拼接前重新 probe 每个归一化文件的真实时长。
  3. **xfade 输出自动协商为 yuv444p**：ffmpeg 7.1 的 xfade 滤镜对 yuv420p 输入也会输出 yuv444p，成片体积明显增大且兼容性差。在滤镜链尾部追加 `[v]format=yuv420p[v]` 强制回 yuv420p（concat_two/concat_three/concat_chain 统一处理）。
  4. **素材音频起点偏移未修正**：原素材音频流起始时间非 0（录制/剪辑常见）会原样带进成片，导致该段声音晚于画面。归一化音频滤镜改为 `asetpts=PTS-STARTPTS,aresample=async=1:first_pts=0`（loudnorm 模式下置于其后）；归一化缓存键升级 normv2 使旧缓存失效。
- 验证：新增 4 个测试（4 段链式转场时长精确 = sum-3×转场、3 段+转场时长精确、归一化保持音视频、流复制保持同步），全套 38 个测试通过；浏览器端到端：4 段任务成片 6.40s（预期 6.5s，误差来自 AAC 帧对齐）、yuv420p、1080×1920 可播放。

## 2026-09-12（R5：多中间素材池，用户可自选池数量）

- 更新：支持添加多个中间素材池（默认 1 个，最多 5 个），成片按池顺序插入头尾之间；每个池可独立选择文件夹、独立勾选固定素材、独立设置随机抽取条数。
- 改动：
  - 引擎（`video_engine.py`）：`JobConfig` 新增 `middle_pools`（每项 {folder, items, count}）；新增 `build_middle_pools` 标准化多池（优先多池、否则回退旧单池字段）；`pick_middle_sequence` 改为按池顺序生成序列（每池固定勾选优先、否则随机抽 count 条，随机种子按池索引错开避免跨池同种子）；`process_batch`/`precheck_materials` 改为多池扫描与校验（固定素材缺失按池报错）；
  - 服务层（`web_app.py`）：`_make_config` 解析 `middle_pools`（≤5 池，folder 存在性校验，items≤10、count 0-10；多池时旧字段取第一池保持兼容）；上传接口新增 `middle_pool[1-5]` 上传根（正则白名单防路径穿越）；
  - 前端（`web/`）：素材库页中间素材区改为池卡片列表——每池独立路径输入/选择/上传/折叠/分批加载/勾选（序号徽标），"+ 添加素材池"按钮（上限 5），池可移除（至少保留 1 个）；参数页"中间素材"卡片改为每池一个随机抽取条数滑杆；"全部展开/收起"同步作用于所有池；
- 修复：删除池后再添加导致池 id 与现存池重复（`length+1` 生成 id 在删除场景下冲突）、Vue 同元素 `v-for`+`v-if` 优先级导致渲染报错（改为 template 包裹）。
- 作用：用户可自由添加 N 个中间素材池并独立配置，成片中间段 = 池1序列 + 池2序列 + …，满足"多加几个中间素材池、自选数量"需求。
- 优化：新增 3 个测试（多池按序插入、单池回退、多池缺失素材校验），全套 34 个测试通过；浏览器实测：添加/移除池、双池独立路径与勾选、参数页每池滑杆、5 片段任务端到端（h+a+b+x+t，结果表中间列显示三条、ZIP、成片可播放）。

## 2026-09-12（R4：中间片段多序列、素材预览折叠、转场库扩充）

- 更新：①中间素材从"三段式单条"升级为"头尾固定 + 中间任意添加多条"；②素材预览默认折叠并分批加载，适配几百条素材；③随机转场支持自定义时长，转场效果从 10 种扩充到 39 种。
- 改动：
  - 引擎（`video_engine.py`）：
    - `JobConfig` 新增 `middle_items`（固定勾选多条，按勾选顺序插入头尾之间，最多 10 条）与 `middle_count`（未勾选时从中间池随机抽取条数，0 表示不插；为 `None` 时兼容旧行为——中间文件夹存在则默认插 1 条）；
    - 新增 `pick_middle_sequence` 统一决策中间序列；新增 `concat_chain` 通用 N 片段拼接（链式 xfade/acrossfade，标签以 `[v]`/`[a]` 收尾）；
    - `_process_one_combo`/`_process_one_item`/`process_batch`/`process_failed_items` 全部改为多中间片段；结果 `middle` 字段改为显示名（多条顿号连接），新增 `middle_files` 路径列表供失败重试；
    - `DEFAULT_TRANSITIONS` 由 10 种扩到 39 种：新增 wipeup/wipedown/circlecrop/smooth×4/diag×4/zoomin/pixelize/fadeblack/fadewhite/cover×4/reveal×4/vertopen/vertclose/horzopen/horzclose/squeezeh/squeezev；
  - 服务层（`web_app.py`）：`_make_config` 接收并校验 `middle_items`（≤10 条）/`middle_count`（0-10，缺省为 None 兼容旧配置），`fixed_middle` 自动并入 `middle_items`；
  - 前端（`web/`）：素材区每类素材默认折叠（显示数量摘要 + 展开按钮），展开后分批渲染（24 个/批 + 加载更多），支持全部展开/收起；中间素材卡片可多选（按点击顺序显示序号徽标，最多 10 条）；参数页新增"中间素材"卡片（随机中间片段数滑杆）与"随机转场时长"输入；随机转场池展示全部 39 种效果（滚动容器）。
- 作用：满足"头尾固定、中间任意添加多条"的拼接需求；几百条素材的文件夹不再一次性渲染全部卡片与缩略图，界面不卡顿；转场时长在随机模式下同样可控，效果选择大幅增加。
- 修复：`concat_chain` 链式滤镜最后输出标签与 `-map [v]/[a]` 不一致导致 FFmpeg 返回码 -22 的问题。
- 优化：新增 6 个测试用例（固定多条中间、随机抽取数量、勾选素材缺失校验、新转场类型可编码、多条中间+转场），全套 31 个测试通过；浏览器实测：折叠/展开、中间多选（序号徽标）、参数页新选项、4 片段任务端到端（结果表中间列显示"m1.mp4、m2.mp4"、ZIP 打包、成片可播放）。

## 2026-09-12（R3 前端重写：Vue3 三步工作台、双主题设计系统）

- 更新：Web 前端整体重写为 Vue3 三步向导工作台（素材库 → 参数配置 → 生成与结果），全部依赖本地化（`web/vendor/vue.global.prod.js`），无外部 CDN、无构建步骤。
- 改动：
  - 新增 `web/style.css` 设计系统：深色/浅色双主题（CSS 变量切换）、侧栏+主区布局、卡片/按钮/分段控件/开关/滑杆/chips/表格/日志面板统一风格；
  - 新增 `web/app.js`：Vue3 单文件应用，素材扫描+详情并发增强（时长/分辨率/横竖/损坏标记）、缩略图加载、文件夹选择/批量上传、1s 轮询任务状态（进度/速度/ETA）、成功失败混合结果表格、ZIP 打包、历史载入配置、模板与配置管理；
  - `web/index.html` 改为三步向导结构，参数配置页整合全部既有功能：出片数量/并发滑杆、分辨率与时长分段、平台预设、文件名模板、智能去重+随机种子、转场（固定/随机+转场池 chips+时长）、BGM（随机/固定+音量+淡入淡出+闪避）、水印（铺满全屏/角落水印+位置/大小/透明度）、统一音量、自动字幕、模板与配置操作条；
  - 静态资源引入版本参数（`?v=20260912`），避免浏览器缓存旧版前端。
- 作用：界面从功能堆砌升级为三步向导式工作台，双主题、可视化素材卡片与实时进度，操作路径更清晰。
- 修复：
  - Vue 白屏：`setup()` 未暴露顶层状态（模板引用 `msg`/`folders` 等裸名报 `Cannot read properties of undefined (reading 'text')`），改为 `...Vue.toRefs(state)` 展开；
  - 任务状态不刷新：`onMounted` 在模块级调用（脱离组件实例上下文被 Vue 丢弃，轮询从未启动），移入 `setup()` 内后 1s 轮询正常工作；
  - 结果表格中间列 v-if/v-else 导致列不对齐，改为固定单元格占位；
  - 缩略图加载失败显示破图，增加 `@error` 兜底。
- 优化：浏览器实测验证三步渲染、素材扫描与缩略图、固定交互、批量生成 3/3 成功、结果表格与下载、ZIP、历史、双主题切换，全套 26 个测试通过。

## 2026-09-12（R2 服务层重构：结构化路由、下载白名单、参数校验、新 API）

- 更新：Web 服务层重构，路由结构化、状态机统一、新增缩略图/预检/打包/素材详情 API。
- 改动：
  - `do_GET`/`do_POST` 的巨型 if 链改为按路径分发，新增 `/style.css`、`/app.js`、`/vendor/vue.global.prod.js` 静态资源路由；
  - 新增下载白名单：`/api/download` 与 `/api/zip` 只允许访问已登记的输出目录（任务/预览/上传根），其余路径一律 403，修复任意路径读取漏洞；
  - 任务启动、预览、失败重试统一走 `_run_task` 状态机，历史记录统一写入一次（修复失败重试时历史双写、同秒时间戳互相覆盖的问题，`save_history` 文件名加毫秒后缀）；
  - `_make_config` 全面参数校验：`transition_duration`、`bgm_volume`、`watermark_scale`、`watermark_opacity`、`count`、`workers` 等非法输入自动回落默认值，不再导致崩溃；
  - 新增 API：`/api/thumb`（素材缩略图）、`/api/material_detail`（素材详情）、`/api/precheck`（批量预检）、`/api/zip`（输出打包下载）；
  - `/api/status` 增加 `eta_seconds`、`speed_per_sec`（基于最近采样点的完成速率估算）；
  - 修复 `status_dict` 持锁时调用带锁方法导致的自死锁（HTTP 请求挂起）。
- 作用：服务更安全、参数更健壮、前端可拿到缩略图/预检/打包/实时速率能力。
- 优化：新增 `tests/test_web_smoke.py` 端到端冒烟（参数校验、真实任务、下载白名单、zip、非法参数），全部通过。

## 2026-09-12（R1 视频引擎重构：缓存、并发、预检、水印参数化）

- 更新：视频处理引擎整体重构，新增归一化缓存、任务级并发、素材预检、水印参数化、缩略图抽帧、concat 流复制快路径。
- 改动：
  - `normalize_clip` 增加缓存层：按文件指纹（路径+大小+修改时间+目标参数）缓存归一化中间文件，跨任务复用，第二次起不再重复转码同一素材；
  - `process_batch` 支持多 worker 并发（`workers` 字段，默认 2，上限 8），并行生成多条成片；
  - 新增 `precheck_materials` 批量预检素材，坏文件提前标记；
  - 水印支持两种模式：`铺满全屏`（保持原行为）和 `角落水印`（可调位置/大小/透明度）；
  - 新增 `get_thumbnail` 素材缩略图抽帧（带缓存）；
  - 无转场拼接改用 concat demuxer 流复制（`concat_copy`），跳过无谓重编码；
  - 归一化输出统一 `-video_track_timescale 15360`，保证流复制拼接参数一致；
  - 日志写入加线程锁，多 worker 并发写日志不再交错丢失。
- 作用：200 条批量任务从"每条多次全量重编码"降为"素材仅转码一次 + 并发执行"，预期提速 3-5 倍；坏素材在批量开始前即可发现；水印满足"右下角小图+透明度"需求。
- 修复：
  - 归一化缓存临时文件以 `.tmp` 结尾导致 FFmpeg 无法识别输出格式（返回码 -22）的问题，临时文件改用 `.mp4` 结尾；
  - 并发分支取消时未标记 `result.cancelled` 的问题。
- 优化：新增 `tests/test_engine_v2.py` 覆盖缓存命中、并发一致性、角落水印、缩略图、预检、concat 快路径，全套 26 个测试通过。

## 2026-09-07（数量显示位置调整）

- 更新：素材数量显示移动到路径输入框后面。
- 改动：调整页面网格布局，把数量提示放在路径框和按钮之间。
- 作用：数量信息紧跟在路径后面，阅读更直观。
- 修复：数量显示位置不理想的问题。
- 优化：开头、结尾、中间、音乐文件夹数量显示统一。


## 2026-09-07（素材自动扫描）

- 更新：移除“扫描素材”按钮。
- 改动：开头、结尾、中间、音乐文件夹路径变化后自动扫描。
- 作用：减少手动操作，选中路径即显示素材数量。
- 修复：手动扫描步骤繁琐的问题。
- 优化：每个文件夹输入框后直接显示识别数量。


## 2026-09-07（BGM 界面重做）

- 更新：BGM 来源简化为一个音乐文件夹。
- 改动：BGM 模式改为“不使用 / 随机 BGM / 固定 BGM”。
- 作用：选择音乐文件夹后可识别 BGM 数量，并选择随机或固定 BGM。
- 修复：移除单文件导入和算法生成入口，避免界面混乱。
- 优化：固定 BGM 通过下拉框选择，符合用户预期。


## 2026-09-07（音乐文件夹 BGM）

- 更新：支持选择整个音乐文件夹作为 BGM 来源。
- 改动：BGM 模式新增“音乐文件夹固定”和“音乐文件夹随机”。
- 作用：可以固定使用一首音乐，也可以每条成片随机使用不同音乐。
- 修复：音乐文件只能单文件导入的问题。
- 优化：自动识别 MP3、WAV、M4A、AAC、FLAC、OGG 文件。


## 2026-09-07（多转场功能）

- 更新：支持多种转场效果。
- 改动：增加固定转场、随机转场、转场类型池和转场时长调节。
- 作用：用户可以选择一种转场固定使用，也可以每条随机使用不同转场。
- 修复：中间素材原本无法应用转场的问题。
- 优化：转场时长支持 0.1–2.0 秒，随机转场使用可复现随机种子。


## 2026-09-07（选择按钮防重复点击）

- 更新：点击“选择”后立即禁用所有选择按钮，选择结束再恢复。
- 改动：前端增加 `selectBusy` 状态，并在请求期间禁用选择按钮。
- 作用：连续点击只能触发一次选择窗口。
- 修复：连续点击“选择”按钮仍然弹出多个窗口的问题。
- 优化：与上传按钮行为一致，避免重复操作。


## 2026-09-07（文件夹选择窗口优化）

- 更新：文件夹选择窗口改为置顶显示。
- 改动：增加单实例锁，同一时间只允许一个选择窗口存在。
- 作用：避免重复点击“选择”按钮弹出多个窗口。
- 修复：重复弹窗问题。
- 优化：选择窗口提示为中文，重复点击时页面提示“文件夹选择窗口已打开，请先关闭”。


## 2026-09-07

- 更新：素材选择改为直接读取本地路径，不再强制复制到 uploads。
- 改动：恢复“选择”按钮，使用系统文件夹选择器；保留“上传”作为备用。
- 作用：减少磁盘占用，避免重复复制大体积视频素材。
- 修复：修复下载中文文件名导致 HTTP 头编码错误的问题。
- 优化：输出路径改为必填，未选择时提示“请选择输出路径”，并记住上次选择。


## 2026-09-06（水印全屏适配）

- 更新：水印由右下角小图改为铺满全屏。
- 改动：水印图片按视频目标分辨率等比放大并裁切，避免拉伸变形。
- 作用：让水印与视频画面比例一致，覆盖整个视频。
- 修复：水印尺寸过小、不铺满全屏的问题。
- 优化：统一水印铺满逻辑，适配竖屏和横屏。

## 2026-09-06（水印上传修复）

- 更新：水印选择改为浏览器原生文件选择标签。
- 改动：水印“上传”按钮改为 `<label for="watermark_upload">`，避免 `input.click()` 在部分浏览器中失效。
- 作用：确保点击“选择PNG”能稳定弹出文件选择窗口。
- 修复：PNG 水印无法加载的问题。
- 优化：水印路径输入框增加示例提示，上传成功后显示文件名。


## 2026-09-06（追加）

- 更新：水印和 BGM 上传后显示明确提示。
- 改动：将页面中的 `alert` 弹窗改为页面内提示信息，避免浏览器拦截导致“没反应”。
- 作用：解决水印加入、保存配置、载入配置、开始生成等按钮在部分浏览器中无反馈的问题。
- 修复：水印/音乐上传后没有可见反馈的问题。
- 优化：提示信息不阻塞操作，6 秒后自动消失。


### 当前版本摘要

- 更新：完成 P0–P12 全部优化阶段。
- 改动：增加配置保存、任务历史、模板管理、平台预设、生成前预览、中间素材池、自动字幕可选插件。
- 作用：让信息流素材拼接工具具备完整的批量生成、配置复用、历史追踪、预览和扩展能力。
- 修复：修复前端缺失多个控件导致“扫描素材、保存配置、载入配置、开始生成”等按钮全部失效的问题。
- 优化：增加扫描失败提示、路径变化自动扫描、断点续跑、失败重试、智能组合去重、音频统一、BGM 淡入淡出和人声闪避。

### 历史提交摘要

- `f543b71` 忽略预览输出目录
- `658afc2` 修复缺失前端控件，增加清空历史记录
- `5da5c6d` P12 自动字幕可选插件
- `e9aa9c8` P11 平台尺寸与质量预设
- `9f784c9` P10 模板管理
- `ce37297` P9 生成前预览
- `24c32f3` P8 中间素材池
- `35dabc9` P7 智能组合与去重
- `9a27b13` P6 输出命名模板
- `a8e0096` P5 音频优化
- `43de814` P4 任务历史
- `fb12a59` P3 结果明细和失败原因
- `b48f2b1` P2 配置保存与加载
- `60ad584` P1 断点续跑、失败重试、日志持久化
- `084c689` P0 基线测试套件